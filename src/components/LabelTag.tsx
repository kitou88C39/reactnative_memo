import { Text, View } from 'react-native';

type LabelTagProps = {
  color: string;
  name: string;
};

const LabelTag: React.FC<LabelTagProps> = props => {
  const { color, name } = props;

  return () => {};
};

export { LabelTag };
